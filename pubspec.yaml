name: rick_morty_cubitapp
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  flutter_svg: ^1.1.5
  dio: ^4.0.6
  flutter_bloc: ^8.1.1
  bloc: ^8.1.0
  equatable: ^2.0.5

dev_dependencies:
  freezed_annotation: ^2.1.0
  json_serializable: ^6.3.2
  build_runner: ^2.2.1
  freezed: ^2.1.1
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: false

  assets:
    - images/
